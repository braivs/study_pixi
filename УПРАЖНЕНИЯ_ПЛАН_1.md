# 🎯 Упражнения PixiJS - План 1 (Базовый путь)

## 📋 Как работать с упражнениями

1. **Читай код внимательно** — понимай, что делает каждая строка
2. **Пиши код сам** — не копируй полностью, используй как подсказку
3. **Экспериментируй** — меняй числа, цвета, размеры
4. **Выполняй задания** — в каждом упражнении есть "Что сделать самому"
5. **Делай по порядку** — каждое упражнение строится на предыдущем

---

## Упражнение 1: Просто показать текст

### Цель
Понять, как создать Pixi приложение и показать текст на экране.

### Код-подсказка
```html
<!DOCTYPE html>
<html>
<head>
    <title>Упражнение 1</title>
</head>
<body>
    <script type="module">
        import * as PIXI from 'https://cdn.skypack.dev/pixi.js';
        
        // 1. Создаем Pixi Application
        const app = new PIXI.Application({
            width: 400,
            height: 300,
            backgroundColor: 0x1a1a2e
        });
        
        // 2. Добавляем canvas на страницу
        document.body.appendChild(app.view);
        
        // 3. Создаем текст
        const text = new PIXI.Text('Hello PixiJS!', {
            fontFamily: 'Arial',
            fontSize: 36,
            fill: '#FFFFFF'
        });
        
        // 4. Добавляем текст на сцену
        app.stage.addChild(text);
    </script>
</body>
</html>
```

### Что сделать самому:
- [ ] Измени текст на свой (например, "Мой первый PixiJS")
- [ ] Измени размер шрифта (попробуй 20, 48, 60)
- [ ] Измени цвет текста (попробуй '#FF0000', '#00FF00', '#FFFF00')
- [ ] Измени цвет фона (попробуй 0x000000, 0xFF0000, 0x222222)
- [ ] Измени размер окна (width и height)

### Вопросы для проверки:
- Что делает `new PIXI.Application()`?
- Что такое `app.stage`?
- Что означает `fill: '#FFFFFF'`?
- Зачем нужен `document.body.appendChild(app.view)`?

---

## Упражнение 2: Движущийся квадрат

### Цель
Понять, как рисовать фигуры и создавать анимацию с помощью ticker.

### Код-подсказка
```html
<!DOCTYPE html>
<html>
<head>
    <title>Упражнение 2</title>
</head>
<body>
    <script type="module">
        import * as PIXI from 'https://cdn.skypack.dev/pixi.js';
        
        const app = new PIXI.Application({
            width: 400,
            height: 300,
            backgroundColor: 0x000000
        });
        document.body.appendChild(app.view);
        
        // Рисуем квадрат
        const square = new PIXI.Graphics();
        square.beginFill(0xFF0000); // Красный цвет
        square.drawRect(0, 0, 50, 50); // x, y, ширина, высота
        square.endFill();
        square.x = 100;
        square.y = 100;
        
        app.stage.addChild(square);
        
        // Двигаем квадрат каждый кадр
        app.ticker.add(() => {
            square.x += 1; // Движется вправо
        });
    </script>
</body>
</html>
```

### Что сделать самому:
- [ ] Измени цвет квадрата (попробуй 0x00FF00, 0x0000FF, 0xFFFF00)
- [ ] Измени размер квадрата (попробуй 30x30, 80x80, 100x50)
- [ ] Измени скорость движения (попробуй 0.5, 2, 5)
- [ ] Сделай квадрат, который движется влево (измени `square.x += 1` на `square.x -= 1`)
- [ ] Сделай квадрат, который движется вниз (измени на `square.y += 1`)
- [ ] Сделай квадрат, который движется по диагонали (двигай и x, и y)
- [ ] Добавь проверку: если квадрат ушел за правый край, верни его налево

### Вопросы для проверки:
- Что делает `new PIXI.Graphics()`?
- Что означает `beginFill(0xFF0000)`?
- Что такое `app.ticker.add()`?
- Как сделать движение быстрее?
- Как сделать движение медленнее?

---

## Упражнение 3: Кнопка, которая что-то делает

### Цель
Понять, как создавать интерактивные элементы (кнопки) и обрабатывать события.

### Код-подсказка
```html
<!DOCTYPE html>
<html>
<head>
    <title>Упражнение 3</title>
</head>
<body>
    <script type="module">
        import * as PIXI from 'https://cdn.skypack.dev/pixi.js';
        
        const app = new PIXI.Application({
            width: 400,
            height: 300,
            backgroundColor: 0x000000
        });
        document.body.appendChild(app.view);
        
        // Рисуем кнопку
        const button = new PIXI.Graphics();
        button.beginFill(0x00ff00); // Зеленый
        button.drawRoundedRect(0, 0, 100, 50, 15);
        button.endFill();
        button.x = 150;
        button.y = 125;
        
        // Добавляем текст на кнопку
        const buttonText = new PIXI.Text('Нажми', {
            fontFamily: 'Arial',
            fontSize: 20,
            fill: '#FFFFFF'
        });
        buttonText.anchor.set(0.5);
        buttonText.x = 50; // Центр кнопки по X
        buttonText.y = 25; // Центр кнопки по Y
        button.addChild(buttonText);
        
        // Включаем интерактивность
        button.eventMode = 'static';
        button.cursor = 'pointer';
        
        // Что происходит при клике
        button.on('pointerdown', () => {
            console.log('Кнопка нажата!');
            buttonText.text = 'Нажато!';
        });
        
        app.stage.addChild(button);
    </script>
</body>
</html>
```

### Что сделать самому:
- [ ] Измени цвет кнопки (попробуй 0xFF0000, 0x0000FF, 0xFFFF00)
- [ ] Измени размер кнопки
- [ ] Измени текст на кнопке
- [ ] Сделай так, чтобы при клике менялся цвет кнопки
- [ ] Создай вторую кнопку, которая делает что-то другое
- [ ] Добавь hover-эффект: при наведении кнопка меняет цвет
  - Подсказка: используй `button.on('pointerenter')` и `button.on('pointerleave')`

### Вопросы для проверки:
- Что означает `eventMode = 'static'`?
- Что такое `cursor = 'pointer'`?
- Что делает `button.on('pointerdown')`?
- Как добавить текст на кнопку?
- Почему `buttonText.x = 50`, а не `buttonText.x = button.x + 50`?

---

## Упражнение 4: Простейший "слот" (один символ)

### Цель
Создать простейший слот: один символ, который меняется при клике.

### Код-подсказка
```html
<!DOCTYPE html>
<html>
<head>
    <title>Упражнение 4</title>
</head>
<body>
    <script type="module">
        import * as PIXI from 'https://cdn.skypack.dev/pixi.js';
        
        const app = new PIXI.Application({
            width: 400,
            height: 300,
            backgroundColor: 0x000000
        });
        document.body.appendChild(app.view);
        
        // Символы для слота
        const symbols = ['🍒', '🍋', '🍊', '🔔', '⭐'];
        
        // Показываем один символ
        const symbolText = new PIXI.Text(symbols[0], {
            fontFamily: 'Arial',
            fontSize: 80,
            fill: '#FFFFFF'
        });
        symbolText.anchor.set(0.5);
        symbolText.x = 200; // Центр экрана
        symbolText.y = 150;
        app.stage.addChild(symbolText);
        
        // Кнопка
        const button = new PIXI.Graphics();
        button.beginFill(0x00FF00);
        button.drawRoundedRect(0, 0, 100, 50, 10);
        button.endFill();
        button.x = 150;
        button.y = 220;
        
        const buttonText = new PIXI.Text('Крутить', {
            fontSize: 20,
            fill: '#FFFFFF'
        });
        buttonText.anchor.set(0.5);
        buttonText.x = 50;
        buttonText.y = 25;
        button.addChild(buttonText);
        
        button.eventMode = 'static';
        button.cursor = 'pointer';
        
        // При клике меняем символ на случайный
        button.on('pointerdown', () => {
            const randomSymbol = symbols[Math.floor(Math.random() * symbols.length)];
            symbolText.text = randomSymbol;
        });
        
        app.stage.addChild(button);
    </script>
</body>
</html>
```

### Что сделать самому:
- [ ] Добавь больше символов в массив (попробуй 🍇, 🍉, 🍌)
- [ ] Измени размер символа
- [ ] Добавь текст "Выпало: [символ]" под символом
- [ ] Создай счетчик: сколько раз крутили (текст "Круток: 0")
- [ ] Добавь проверку: если выпал определенный символ (например, ⭐), покажи "ВЫИГРЫШ!"

### Вопросы для проверки:
- Как получить случайный элемент из массива?
- Что делает `Math.floor(Math.random() * symbols.length)`?
- Как изменить текст объекта после создания?

---

## Упражнение 5: Три символа (простой слот)

### Цель
Создать слот с тремя символами, которые меняются при клике.

### Код-подсказка
```html
<!DOCTYPE html>
<html>
<head>
    <title>Упражнение 5</title>
</head>
<body>
    <script type="module">
        import * as PIXI from 'https://cdn.skypack.dev/pixi.js';
        
        const app = new PIXI.Application({
            width: 400,
            height: 300,
            backgroundColor: 0x000000
        });
        document.body.appendChild(app.view);
        
        const symbols = ['🍒', '🍋', '🍊', '🔔', '⭐'];
        
        // Создаем 3 символа
        const symbol1 = new PIXI.Text(symbols[0], {
            fontSize: 60,
            fill: '#FFFFFF'
        });
        const symbol2 = new PIXI.Text(symbols[0], {
            fontSize: 60,
            fill: '#FFFFFF'
        });
        const symbol3 = new PIXI.Text(symbols[0], {
            fontSize: 60,
            fill: '#FFFFFF'
        });
        
        // Центрируем символы
        symbol1.anchor.set(0.5);
        symbol2.anchor.set(0.5);
        symbol3.anchor.set(0.5);
        
        // Размещаем рядом друг с другом
        symbol1.x = 100;
        symbol2.x = 200;
        symbol3.x = 300;
        symbol1.y = 150;
        symbol2.y = 150;
        symbol3.y = 150;
        
        app.stage.addChild(symbol1, symbol2, symbol3);
        
        // Кнопка
        const button = new PIXI.Graphics();
        button.beginFill(0x00FF00);
        button.drawRoundedRect(0, 0, 100, 50, 10);
        button.endFill();
        button.x = 150;
        button.y = 220;
        
        const buttonText = new PIXI.Text('Крутить', {
            fontSize: 20,
            fill: '#FFFFFF'
        });
        buttonText.anchor.set(0.5);
        buttonText.x = 50;
        buttonText.y = 25;
        button.addChild(buttonText);
        
        button.eventMode = 'static';
        button.cursor = 'pointer';
        
        // При клике меняем все символы
        button.on('pointerdown', () => {
            symbol1.text = symbols[Math.floor(Math.random() * symbols.length)];
            symbol2.text = symbols[Math.floor(Math.random() * symbols.length)];
            symbol3.text = symbols[Math.floor(Math.random() * symbols.length)];
            
            // Проверяем выигрыш
            if (symbol1.text === symbol2.text && symbol2.text === symbol3.text) {
                console.log('ВЫИГРЫШ!');
            }
        });
        
        app.stage.addChild(button);
    </script>
</body>
</html>
```

### Что сделать самому:
- [ ] Измени расстояние между символами (сделай их ближе или дальше)
- [ ] Добавь текст "ВЫИГРЫШ!" на экран при выигрыше (вместо console.log)
- [ ] Добавь проверку: если два символа одинаковые, покажи "Частичный выигрыш"
- [ ] Создай разные выплаты: 3 одинаковых = большой выигрыш, 2 одинаковых = маленький
- [ ] Добавь счетчик баланса (начни с 1000, при выигрыше добавляй)

### Вопросы для проверки:
- Как разместить объекты рядом друг с другом?
- Как проверить, что все три символа одинаковые?
- Как показать текст на экране вместо console.log?

---

## Упражнение 6: Добавляем простую анимацию

### Цель
Создать анимацию: символ движется вниз, как в настоящем слоте.

### Код-подсказка
```html
<!DOCTYPE html>
<html>
<head>
    <title>Упражнение 6</title>
</head>
<body>
    <script type="module">
        import * as PIXI from 'https://cdn.skypack.dev/pixi.js';
        
        const app = new PIXI.Application({
            width: 400,
            height: 300,
            backgroundColor: 0x000000
        });
        document.body.appendChild(app.view);
        
        const symbols = ['🍒', '🍋', '🍊', '🔔', '⭐'];
        
        const symbol = new PIXI.Text(symbols[0], {
            fontSize: 60,
            fill: '#FFFFFF'
        });
        symbol.anchor.set(0.5);
        symbol.x = 200;
        symbol.y = 50; // Начинаем сверху
        
        app.stage.addChild(symbol);
        
        let isSpinning = false; // Флаг: крутится ли слот
        let targetY = 150; // Куда должен доехать символ
        
        // Кнопка
        const button = new PIXI.Graphics();
        button.beginFill(0x00FF00);
        button.drawRoundedRect(0, 0, 100, 50, 10);
        button.endFill();
        button.x = 150;
        button.y = 220;
        
        const buttonText = new PIXI.Text('Крутить', {
            fontSize: 20,
            fill: '#FFFFFF'
        });
        buttonText.anchor.set(0.5);
        buttonText.x = 50;
        buttonText.y = 25;
        button.addChild(buttonText);
        
        button.eventMode = 'static';
        button.cursor = 'pointer';
        
        button.on('pointerdown', () => {
            if (!isSpinning) { // Нельзя крутить, если уже крутится
                isSpinning = true;
                symbol.y = 50; // Возвращаем наверх
                targetY = 150;
                symbol.text = symbols[Math.floor(Math.random() * symbols.length)];
            }
        });
        
        // Анимация: двигаем символ вниз
        app.ticker.add(() => {
            if (isSpinning && symbol.y < targetY) {
                symbol.y += 5; // Двигаем вниз
            } else if (isSpinning) {
                isSpinning = false; // Остановились
            }
        });
        
        app.stage.addChild(button);
    </script>
</body>
</html>
```

### Что сделать самому:
- [ ] Измени скорость движения (попробуй 2, 10, 20)
- [ ] Сделай так, чтобы символ двигался быстрее в начале и медленнее в конце
- [ ] Добавь несколько символов, которые движутся друг за другом
- [ ] Сделай так, чтобы символ менялся во время движения (каждый кадр новый случайный)
- [ ] Добавь проверку: когда символ остановился, проверь выигрыш

### Вопросы для проверки:
- Что делает `isSpinning`?
- Зачем нужна проверка `if (!isSpinning)`?
- Как сделать движение плавнее?
- Как сделать движение быстрее?

---

## Упражнение 7: Три рельса с анимацией

### Цель
Создать три рельса, которые останавливаются по очереди (каскадно).

### Код-подсказка
```html
<!DOCTYPE html>
<html>
<head>
    <title>Упражнение 7</title>
</head>
<body>
    <script type="module">
        import * as PIXI from 'https://cdn.skypack.dev/pixi.js';
        
        const app = new PIXI.Application({
            width: 500,
            height: 400,
            backgroundColor: 0x000000
        });
        document.body.appendChild(app.view);
        
        const symbols = ['🍒', '🍋', '🍊', '🔔', '⭐'];
        
        // Создаем 3 символа (по одному на рельс)
        const reel1 = new PIXI.Text(symbols[0], { fontSize: 60, fill: '#FFFFFF' });
        const reel2 = new PIXI.Text(symbols[0], { fontSize: 60, fill: '#FFFFFF' });
        const reel3 = new PIXI.Text(symbols[0], { fontSize: 60, fill: '#FFFFFF' });
        
        reel1.anchor.set(0.5);
        reel2.anchor.set(0.5);
        reel3.anchor.set(0.5);
        
        reel1.x = 150;
        reel2.x = 250;
        reel3.x = 350;
        reel1.y = 150;
        reel2.y = 150;
        reel3.y = 150;
        
        app.stage.addChild(reel1, reel2, reel3);
        
        // Состояние каждого рельса
        const reels = [
            { symbol: reel1, spinning: false, y: 150, targetY: 150 },
            { symbol: reel2, spinning: false, y: 150, targetY: 150 },
            { symbol: reel3, spinning: false, y: 150, targetY: 150 }
        ];
        
        // Кнопка
        const button = new PIXI.Graphics();
        button.beginFill(0x00FF00);
        button.drawRoundedRect(0, 0, 120, 50, 10);
        button.endFill();
        button.x = 190;
        button.y = 300;
        
        const buttonText = new PIXI.Text('SPIN', {
            fontSize: 24,
            fill: '#FFFFFF',
            fontWeight: 'bold'
        });
        buttonText.anchor.set(0.5);
        buttonText.x = 60;
        buttonText.y = 25;
        button.addChild(buttonText);
        
        button.eventMode = 'static';
        button.cursor = 'pointer';
        
        let canSpin = true;
        
        button.on('pointerdown', () => {
            if (!canSpin) return;
            
            canSpin = false;
            
            // Запускаем все рельсы
            reels.forEach((reel, index) => {
                reel.spinning = true;
                reel.y = 50;
                reel.targetY = 150;
                reel.symbol.y = 50;
                
                // Каждый следующий рельс останавливается позже
                setTimeout(() => {
                    reel.targetY = 150;
                    reel.symbol.text = symbols[Math.floor(Math.random() * symbols.length)];
                }, index * 500); // 0ms, 500ms, 1000ms
            });
        });
        
        // Анимация
        app.ticker.add(() => {
            let allStopped = true;
            
            reels.forEach((reel) => {
                if (reel.spinning && reel.y < reel.targetY) {
                    reel.y += 5;
                    reel.symbol.y = reel.y;
                    allStopped = false;
                } else if (reel.spinning) {
                    reel.spinning = false;
                }
            });
            
            if (allStopped && !canSpin) {
                canSpin = true;
                
                // Проверяем выигрыш
                if (reels[0].symbol.text === reels[1].symbol.text && 
                    reels[1].symbol.text === reels[2].symbol.text) {
                    console.log('ВЫИГРЫШ!');
                }
            }
        });
        
        app.stage.addChild(button);
    </script>
</body>
</html>
```

### Что сделать самому:
- [ ] Измени задержку между остановками рельсов (попробуй 300ms, 700ms)
- [ ] Добавь текст "ВЫИГРЫШ!" на экран при выигрыше
- [ ] Добавь счетчик баланса и ставки
- [ ] Сделай так, чтобы при выигрыше баланс увеличивался
- [ ] Добавь проверку: нельзя крутить, если баланс меньше ставки

### Вопросы для проверки:
- Как сделать каскадную остановку рельсов?
- Зачем нужен `canSpin`?
- Как проверить, что все рельсы остановились?

---

## 📝 Чеклист прогресса

Отмечай выполненные упражнения:

- [ ] Упражнение 1: Показал текст на экране
- [ ] Упражнение 2: Создал движущийся квадрат
- [ ] Упражнение 3: Создал кнопку, которая работает
- [ ] Упражнение 4: Простейший слот (один символ)
- [ ] Упражнение 5: Три символа (простой слот)
- [ ] Упражнение 6: Добавил анимацию движения
- [ ] Упражнение 7: Три рельса с каскадной остановкой

---

## 🎯 Что делать после упражнений

Когда выполнишь все упражнения:

1. **Вернись к сложному коду** (`SlotGame.js`)
2. **Попробуй понять метод `createReels()`** — теперь ты знаешь основы
3. **Попробуй понять метод `animateReels()`** — ты уже делал анимацию
4. **Создай свой мини-слот** с нуля, используя то, что изучил

---

## 💡 Советы

- **Не копируй код** — пиши сам, используя примеры как подсказки
- **Экспериментируй** — меняй все параметры и смотри результат
- **Выполняй задания** — в каждом упражнении есть "Что сделать самому"
- **Задавай вопросы** — если что-то непонятно
- **Делай перерывы** — мозгу нужно время на усвоение

---

**Удачи! 🚀**

