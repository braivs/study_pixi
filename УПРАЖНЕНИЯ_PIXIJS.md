# 🎯 Базовые упражнения PixiJS

## 📋 Как работать с упражнениями

1. **Не копируй код полностью** — читай, понимай, пиши сам
2. **Экспериментируй** — меняй числа, цвета, размеры
3. **Задавай вопросы** — если что-то непонятно
4. **Делай по порядку** — каждое упражнение строится на предыдущем

---

## Упражнение 1: Первый текст на экране

### Задача
Создай HTML файл и покажи текст "Привет, PixiJS!" на экране.

### Шаблон кода
```html
<!DOCTYPE html>
<html>
<head>
    <title>Упражнение 1</title>
</head>
<body>
    <script type="module">
        import * as PIXI from 'https://cdn.skypack.dev/pixi.js';
        
        // 1. Создаем приложение
        const app = new PIXI.Application({
            width: 400,
            height: 300,
            backgroundColor: 0x000000  // Черный фон
        });
        
        // 2. Добавляем canvas на страницу
        document.body.appendChild(app.view);
        
        // 3. Создаем текст
        const text = new PIXI.Text('Привет, PixiJS!', {
            fontSize: 36,
            fill: '#FFFFFF'  // Белый цвет
        });
        
        // 4. Добавляем текст на сцену
        app.stage.addChild(text);
    </script>
</body>
</html>
```

### Что сделать самому:
- [ ] Измени текст на свой
- [ ] Измени размер шрифта (попробуй 20, 48, 60)
- [ ] Измени цвет текста (попробуй '#FF0000', '#00FF00', '#0000FF')
- [ ] Измени цвет фона (попробуй 0xFFFFFF, 0xFF0000, 0x222222)

### Вопросы для проверки:
- Что делает `fontSize`?
- Что означает `fill: '#FFFFFF'`?
- Что такое `app.stage`?

---

## Упражнение 2: Позиционирование текста

### Задача
Размести текст в разных местах экрана, используя `x` и `y`.

### Что нужно знать:
- `x` — позиция слева (0 = самый левый край)
- `y` — позиция сверху (0 = самый верх)
- `app.screen.width` — ширина экрана
- `app.screen.height` — высота экрана

### Задание:
Создай 4 текста:
1. В левом верхнем углу (x = 10, y = 10)
2. В правом верхнем углу (x = ширина экрана - 100, y = 10)
3. В левом нижнем углу (x = 10, y = высота экрана - 50)
4. В правом нижнем углу (x = ширина - 100, y = высота - 50)

### Шаблон:
```html
<script type="module">
    import * as PIXI from 'https://cdn.skypack.dev/pixi.js';
    
    const app = new PIXI.Application({
        width: 600,
        height: 400,
        backgroundColor: 0x222222
    });
    document.body.appendChild(app.view);
    
    // Текст 1: Левый верхний угол
    const text1 = new PIXI.Text('Левый верхний', {
        fontSize: 24,
        fill: '#FFFFFF'
    });
    text1.x = 10;  // ← Ты должен написать это сам
    text1.y = 10;  // ← Ты должен написать это сам
    app.stage.addChild(text1);
    
    // Текст 2: Правый верхний угол
    // ← Напиши код сам!
    
    // Текст 3: Левый нижний угол
    // ← Напиши код сам!
    
    // Текст 4: Правый нижний угол
    // ← Напиши код сам!
</script>
```

### Что сделать самому:
- [ ] Напиши код для всех 4 текстов
- [ ] Попробуй разные значения x и y
- [ ] Используй `app.screen.width` и `app.screen.height` для вычисления позиций

### Вопросы для проверки:
- Что произойдет, если `x = 0`?
- Что произойдет, если `x = 1000` (больше ширины экрана)?
- Как вычислить позицию справа, если ширина экрана 600, а текст шириной 100?

---

## Упражнение 3: Центрирование текста

### Задача
Научись центрировать текст с помощью `anchor`.

### Что нужно знать:
- `anchor` — точка привязки текста
- `anchor.set(0, 0)` — левый верхний угол (по умолчанию)
- `anchor.set(0.5, 0.5)` — центр текста
- `anchor.set(1, 1)` — правый нижний угол

### Задание:
Создай текст "ЦЕНТР" точно в центре экрана.

### Подсказка:
```javascript
const text = new PIXI.Text('ЦЕНТР', {
    fontSize: 36,
    fill: '#FFFFFF'
});

text.anchor.set(0.5);  // Центрируем якорь
text.x = app.screen.width / 2;   // Половина ширины
text.y = app.screen.height / 2;  // Половина высоты

app.stage.addChild(text);
```

### Что сделать самому:
- [ ] Создай текст в центре экрана
- [ ] Попробуй `anchor.set(0.5, 0)` — что происходит?
- [ ] Попробуй `anchor.set(0, 0.5)` — что происходит?
- [ ] Создай текст внизу по центру (используй `anchor.set(0.5, 1)`)

### Вопросы для проверки:
- Зачем нужен `anchor`?
- В чем разница между `anchor.set(0.5)` и `anchor.set(0.5, 0.5)`?
- Как разместить текст внизу по центру?

---

## Упражнение 4: Движущийся текст

### Задача
Создай текст, который движется по экрану.

### Что нужно знать:
- `app.ticker.add()` — функция вызывается каждый кадр (60 раз в секунду)
- Внутри функции можно менять `x` и `y` текста

### Задание:
Создай текст, который движется слева направо.

### Шаблон:
```html
<script type="module">
    import * as PIXI from 'https://cdn.skypack.dev/pixi.js';
    
    const app = new PIXI.Application({
        width: 600,
        height: 400,
        backgroundColor: 0x222222
    });
    document.body.appendChild(app.view);
    
    const text = new PIXI.Text('Двигаюсь!', {
        fontSize: 36,
        fill: '#FFFFFF'
    });
    text.anchor.set(0.5);
    text.x = 50;   // Начинаем слева
    text.y = 200;  // По центру по вертикали
    app.stage.addChild(text);
    
    // Анимация: вызывается каждый кадр
    app.ticker.add(() => {
        text.x += 2;  // Двигаем вправо на 2 пикселя
        
        // Если ушли за правый край, возвращаем налево
        if (text.x > app.screen.width + 50) {
            text.x = -50;
        }
    });
</script>
```

### Что сделать самому:
- [ ] Измени скорость движения (попробуй 1, 5, 10)
- [ ] Сделай текст, который движется сверху вниз
- [ ] Сделай текст, который движется по диагонали
- [ ] Сделай текст, который движется туда-сюда (влево-вправо)

### Вопросы для проверки:
- Что произойдет, если убрать проверку `if (text.x > ...)`?
- Как сделать движение быстрее?
- Как сделать движение медленнее?

---

## Упражнение 5: Текст, который следует за мышью

### Задача
Создай текст, который всегда находится там, где курсор мыши.

### Что нужно знать:
- `eventMode = 'static'` — включает события
- `hitArea` — область, где работают события
- `pointermove` — событие движения мыши
- `event.globalX` и `event.globalY` — координаты мыши

### Задание:
Создай текст "Следуй за мной", который следует за курсором.

### Шаблон:
```html
<script type="module">
    import * as PIXI from 'https://cdn.skypack.dev/pixi.js';
    
    const app = new PIXI.Application({
        width: 600,
        height: 400,
        backgroundColor: 0x222222
    });
    document.body.appendChild(app.view);
    
    const text = new PIXI.Text('Следуй за мной', {
        fontSize: 24,
        fill: '#FFFFFF'
    });
    text.anchor.set(0.5);
    app.stage.addChild(text);
    
    // Включаем события
    app.stage.eventMode = 'static';
    app.stage.hitArea = app.screen;
    
    // Когда мышь двигается
    app.stage.on('pointermove', (event) => {
        text.x = event.globalX;
        text.y = event.globalY;
    });
</script>
```

### Что сделать самому:
- [ ] Измени текст на свой
- [ ] Добавь отступ от курсора (например, `text.x = event.globalX + 20`)
- [ ] Создай второй текст, который следует за мышью с задержкой

### Вопросы для проверки:
- Что такое `event.globalX`?
- Что произойдет, если не установить `hitArea`?
- Как сделать, чтобы текст был не на курсоре, а немного в стороне?

---

## Упражнение 6: Простая кнопка

### Задача
Создай кнопку из графики и текст на ней.

### Что нужно знать:
- `PIXI.Graphics` — для рисования фигур
- `beginFill()` — начать заливку
- `drawRoundedRect()` — нарисовать скругленный прямоугольник
- `endFill()` — закончить заливку
- `addChild()` — добавить объект в контейнер

### Задание:
Создай зеленую кнопку с текстом "Нажми меня".

### Шаблон:
```html
<script type="module">
    import * as PIXI from 'https://cdn.skypack.dev/pixi.js';
    
    const app = new PIXI.Application({
        width: 600,
        height: 400,
        backgroundColor: 0x222222
    });
    document.body.appendChild(app.view);
    
    // Рисуем кнопку
    const button = new PIXI.Graphics();
    button.beginFill(0x00FF00);  // Зеленый цвет
    button.drawRoundedRect(0, 0, 150, 60, 10);  // x, y, ширина, высота, скругление
    button.endFill();
    button.x = 225;  // Позиция кнопки
    button.y = 170;
    
    // Добавляем текст на кнопку
    const buttonText = new PIXI.Text('Нажми меня', {
        fontSize: 20,
        fill: '#000000'  // Черный текст
    });
    buttonText.anchor.set(0.5);
    buttonText.x = 75;   // Центр кнопки по X
    buttonText.y = 30;   // Центр кнопки по Y
    button.addChild(buttonText);
    
    app.stage.addChild(button);
</script>
```

### Что сделать самому:
- [ ] Измени цвет кнопки (попробуй 0xFF0000, 0x0000FF, 0xFFFF00)
- [ ] Измени размер кнопки
- [ ] Измени текст на кнопке
- [ ] Создай вторую кнопку другого цвета

### Вопросы для проверки:
- Что означает `0x00FF00`?
- Почему `buttonText.x = 75`, а не `buttonText.x = button.x + 75`?
- Как изменить форму кнопки (сделать круглой)?

---

## Упражнение 7: Кнопка, которая что-то делает

### Задача
Создай кнопку, которая меняет текст при клике.

### Что нужно знать:
- `eventMode = 'static'` — включает события
- `cursor = 'pointer'` — курсор-указатель при наведении
- `on('pointerdown')` — событие клика
- `text.text = 'новый текст'` — изменить текст

### Задание:
Создай кнопку и текст. При клике на кнопку текст должен меняться.

### Шаблон:
```html
<script type="module">
    import * as PIXI from 'https://cdn.skypack.dev/pixi.js';
    
    const app = new PIXI.Application({
        width: 600,
        height: 400,
        backgroundColor: 0x222222
    });
    document.body.appendChild(app.view);
    
    // Текст, который будем менять
    const text = new PIXI.Text('Нажми кнопку!', {
        fontSize: 32,
        fill: '#FFFFFF'
    });
    text.anchor.set(0.5);
    text.x = app.screen.width / 2;
    text.y = 150;
    app.stage.addChild(text);
    
    // Кнопка
    const button = new PIXI.Graphics();
    button.beginFill(0x00FF00);
    button.drawRoundedRect(0, 0, 150, 60, 10);
    button.endFill();
    button.x = 225;
    button.y = 250;
    
    const buttonText = new PIXI.Text('Нажми', {
        fontSize: 20,
        fill: '#000000'
    });
    buttonText.anchor.set(0.5);
    buttonText.x = 75;
    buttonText.y = 30;
    button.addChild(buttonText);
    
    // Включаем клики
    button.eventMode = 'static';
    button.cursor = 'pointer';
    
    // Что происходит при клике
    button.on('pointerdown', () => {
        text.text = 'Кнопка нажата!';
        text.style.fill = '#00FF00';  // Меняем цвет текста
    });
    
    app.stage.addChild(button);
</script>
```

### Что сделать самому:
- [ ] Создай кнопку, которая меняет текст на случайный
- [ ] Создай кнопку, которая меняет цвет текста
- [ ] Создай кнопку, которая двигает текст
- [ ] Создай две кнопки: одна меняет текст, другая меняет цвет

### Вопросы для проверки:
- Что произойдет, если не установить `eventMode = 'static'`?
- Как сделать, чтобы при каждом клике текст менялся по-разному?
- Как сделать кнопку, которая считает клики?

---

## Упражнение 8: Простейший "слот" (без анимации)

### Задача
Создай три символа, которые меняются при клике на кнопку.

### Задание:
1. Создай 3 символа (например, 🍒, 🍋, 🍊)
2. Размести их рядом друг с другом
3. Создай кнопку "Крутить"
4. При клике все символы должны меняться на случайные

### Подсказка:
```javascript
const symbols = ['🍒', '🍋', '🍊', '🔔', '⭐'];

// Случайный символ:
const randomSymbol = symbols[Math.floor(Math.random() * symbols.length)];
```

### Что сделать самому:
- [ ] Напиши код полностью сам
- [ ] Добавь проверку выигрыша (если все 3 одинаковые)
- [ ] Добавь текст "ВЫИГРЫШ!" при выигрыше
- [ ] Добавь больше символов в массив

### Вопросы для проверки:
- Как получить случайный элемент из массива?
- Как проверить, что все три символа одинаковые?
- Как разместить символы рядом друг с другом?

---

## Упражнение 9: Текст, который двигается по кругу

### Задача
Создай текст, который движется по кругу вокруг центра экрана.

### Что нужно знать:
- `Math.sin()` и `Math.cos()` — тригонометрические функции
- Угол измеряется в радианах (2π = 360°)

### Задание:
Создай текст, который вращается по кругу.

### Шаблон:
```html
<script type="module">
    import * as PIXI from 'https://cdn.skypack.dev/pixi.js';
    
    const app = new PIXI.Application({
        width: 600,
        height: 400,
        backgroundColor: 0x222222
    });
    document.body.appendChild(app.view);
    
    const text = new PIXI.Text('Круг', {
        fontSize: 24,
        fill: '#FFFFFF'
    });
    text.anchor.set(0.5);
    app.stage.addChild(text);
    
    let angle = 0;  // Угол в радианах
    const radius = 100;  // Радиус круга
    const centerX = app.screen.width / 2;
    const centerY = app.screen.height / 2;
    
    app.ticker.add(() => {
        angle += 0.02;  // Увеличиваем угол
        
        // Вычисляем позицию по кругу
        text.x = centerX + Math.cos(angle) * radius;
        text.y = centerY + Math.sin(angle) * radius;
    });
</script>
```

### Что сделать самому:
- [ ] Измени радиус круга (попробуй 50, 150, 200)
- [ ] Измени скорость вращения (измени 0.02)
- [ ] Создай два текста, вращающихся в разные стороны
- [ ] Создай текст, который вращается по эллипсу (разные радиусы для X и Y)

### Вопросы для проверки:
- Что произойдет, если увеличить `radius`?
- Что произойдет, если увеличить `0.02`?
- Как сделать вращение в обратную сторону?

---

## Упражнение 10: Создай свой интерфейс

### Задача
Создай интерфейс игры со слотами (без функционала, только визуал).

### Задание:
Создай экран с:
1. Заголовок "СЛОТ-МАШИНА" сверху по центру (большой, золотой)
2. "Баланс: 1000" слева вверху
3. "Ставка: 10" справа вверху
4. Три символа по центру (🍒 🍒 🍒)
5. Кнопка "SPIN" внизу по центру

### Что сделать самому:
- [ ] Напиши весь код сам
- [ ] Используй разные цвета для разных элементов
- [ ] Размести все элементы правильно
- [ ] Сделай кнопку красивой (с обводкой, скруглениями)

### Вопросы для проверки:
- Как вычислить позицию для центрирования?
- Как разместить текст справа?
- Как сделать красивую кнопку?

---

## 📝 Чеклист прогресса

Отмечай выполненные упражнения:

- [ ] Упражнение 1: Показал текст на экране
- [ ] Упражнение 2: Разместил текст в разных углах
- [ ] Упражнение 3: Центрировал текст
- [ ] Упражнение 4: Сделал движущийся текст
- [ ] Упражнение 5: Текст следует за мышью
- [ ] Упражнение 6: Создал кнопку
- [ ] Упражнение 7: Кнопка что-то делает
- [ ] Упражнение 8: Простейший слот
- [ ] Упражнение 9: Текст по кругу
- [ ] Упражнение 10: Свой интерфейс

---

## 🎯 Что делать после упражнений

Когда выполнишь все упражнения:

1. **Вернись к сложному коду** (`SlotGame.js`)
2. **Попробуй понять одну функцию** (например, `setupUI()`)
3. **Измени что-то** в коде и посмотри, что происходит
4. **Создай свой мини-слот** с нуля

---

## 💡 Советы

- **Не копируй код** — пиши сам, используя примеры как подсказки
- **Экспериментируй** — меняй все параметры и смотри результат
- **Задавай вопросы** — если что-то непонятно
- **Делай перерывы** — мозгу нужно время на усвоение

---

**Удачи! 🚀**

